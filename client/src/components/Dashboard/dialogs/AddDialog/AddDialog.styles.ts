import styled from 'styled-components';
import Dialog from '@mui/material/Dialog';

export const SAddDialogContainer = styled.div``;

export const SDialog = styled(Dialog)`
    display: flex;
    justify-content: center;
    align-items: center;
    padding-right: 14px;
`;
